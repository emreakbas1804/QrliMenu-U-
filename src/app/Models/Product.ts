export interface Product{
  categoryId: number,
  productDescription: string,
  productId: number,
  productImageUrl: string,
  productIsActive: boolean,
  productName: string,
  productPrice: string,
  userId: string
}
